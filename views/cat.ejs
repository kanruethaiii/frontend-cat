<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cat</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<center>
<body>
  <h1><%= cat.name %></h1>
  <p><strong>Breed:</strong> <%= cat.breed %></p>
  <p><strong>Color:</strong> <%= cat.color %></p>
  <p><strong>Price:</strong> <%= cat.price %></p>
  <p><strong>Availability:</strong> <%= cat.availability ? "Available" : "Not Available" %></p>

  <a href="/update/<%= cat.id %>">Edit</a>
  <a href="/delete/<%= cat.id %>" onclick="return confirm('Are you sure you want to delete this cat?')">Delete</a>
  
  <br><br>
  <!-- Corrected Back to List link -->
  <a href="/" class="CreateNewCat">Back to List</a>
</body>
</center>
</html>