<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cat Shop</title>
    <link rel="stylesheet"  href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <ul>
            <li><a href="/cat">Cat</a></li>
            <li><a href="/customer">Customer</a></li>
            <li><a href="/order">Order</a></li>
            <li><a href="/breed">Breed</a></li>
        </ul>
    </nav>
    <main>
        <h1>Breed</h1>
        <center>
        <table class="list" style="width: 50%;">
            <thead>
                <tr>
                    <th>No</th>
                    <th>Name</th>
                    <th>Active</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% let no = 1 %>
                <% breeds.forEach(breed => { %>
                    <tr>
                        <td><%= no %></td>
                        <td><%= breed.name %></td>
                        <td><%= breed.is_active ? "Active" : "Inactive" %></td>
                        <td>
                            <a href="/breed/<%= breed.id %>">View</a>
                            <a href="/breed/<%= breed.id %>/edit">Edit</a>
                            <a href="/breed/<%= breed.id %>/delete" onclick="return confirm('Are you sure you want to delete this item?')">Delete</a>
                        </td>
                    </tr>
                    <% no++ %>
                <% }) %>
            </tbody>
        </table>
        <a href="/breed/create" class="CreateNewCat" ">Create New</a>
    </center>
    </main>
</body>
</html>
