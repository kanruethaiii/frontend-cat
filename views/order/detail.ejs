<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cat</title>
  <link rel="stylesheet" href="/css/view.css">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
</head>
<center>
<body>
  <div class="container">
    <h1>Order</h1>
    <table class="order-table">
      <tr>
        <th>Order ID</th>
        <td><%= "O" + String(order.id).padStart(3, '0') %></td>
      </tr>
      <tr>
        <th>Customer</th>
        <td><%= order.customer_name %></td>
      </tr>
      <tr>
        <th>Cat</th>
        <td><%= order.cat_name %></td>
      </tr>
      <tr>
        <th>Date</th>
        <td><%= new Date(order.order_date).toLocaleDateString('en-GB') %></td>
      </tr>
      <tr>
        <th>Quantity</th>
        <td><%= order.quantity %></td>
      </tr>
      <tr>
        <th>Price</th>
        <td><%= order.unitPrice %></td>
      </tr>
      <tr>
        <th>Total</th>
        <td><%= order.total_amount %></td>
      </tr>
    </table>
  </div>
  
  
  
  <br><br>
  <!-- Corrected Back to List link -->
  <a href="/order/<%= order.id %>/edit" class="button" >Edit</a>
  <a href="/order/<%= order.id %>/delete" onclick="return confirm('Are you sure you want to delete this item?')" class="button">Delete</a>
  <a href="/order" class="button">Back to List</a>

</body>
</center>
</html>